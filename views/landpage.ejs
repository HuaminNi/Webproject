<%- include("partials/header") %>
    <h1>HI this is a ejs land page!</h1>
    
    <% camps.forEach((camp) => { %>
        <div>
            <h4><%= camp.name %></h4>
            <img src="<%= camp.image %>">
        </div>
        
   <% }) %>
    
   <%- include("partials/footer") %>